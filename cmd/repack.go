package cmd

/*
import (
	"github.com/spf13/cobra"
)

var repack_dontPrintProcess *bool

var repackCmd = &cobra.Command{
	Use:   "repack",
	Short: "Repacks the given mods",
	Long:  ``,
	Args:  cobra.RangeArgs(1, 100),
	RunE: func(cmd *cobra.Command, args []string) error {
		return modHandler.RepackMods(!*repack_dontPrintProcess, args...)
	},
}

func init() {
	repack_dontPrintProcess = repackCmd.Flags().Bool("np", false, "Don't print the repacking process messages")

	rootCmd.AddCommand(repackCmd)
}
*/
